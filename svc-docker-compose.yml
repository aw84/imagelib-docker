version: "3.7"
services:
  api:
    depends_on:
      - "config"
    image: image-base
    ports:
      - 9000:8080
    volumes:
      - ./jars:/jars
    environment:
      - ENCRYPTION_KEY=${ABCX}
    env_file:
      - ./image-api.env
    networks:
      - imagelib_network
  config:
    image: image-base
    ports:
      - 9001:8082
    volumes:
      - ./jars:/jars
    environment:
      - ENCRYPTION_KEY=${ABCX}
    env_file:
      - ./image-config-server.env
    networks:
      - imagelib_network
networks:
  imagelib_network:
    external:
      name: imagelib_network
